/**
 * Configuración de entorno.
 *
 * `apiGatewayUrl` es el punto único de entrada (API Gateway - Quarkus).
 */
export const environment = {
  production: false,
  apiGatewayUrl: 'http://localhost:8080',
};
